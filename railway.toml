# Use Railway's default Python builder
# It will look for requirements.txt and run the start command
[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
